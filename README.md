## 环境设置

使用WSL2，Ubuntu 22.04.5 LTS

## 使用方式

​    $ git clone <本仓库url>
​    $ make

将你的输入写入 text8_BefSeg 文件中，然后运行：

​    $ ./demo.sh

Make sure you have the following prerequisites installed when running the steps above:

* GNU Make
* GCC (Clang pretending to be GCC is fine)
* Python and NumPy